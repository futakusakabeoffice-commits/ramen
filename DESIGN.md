# Visual Design System (DESIGN.md)

本ファイルは、AIエージェントがUIコード（HTML, CSS, React, など）を生成する際に参照するデザイン仕様書です。UIを構築する際は、必ずこのルールに従ってください。

対象業種：ラーメン店（個人経営〜小規模店舗）

## 1. トーン＆マナー
* **ブランドイメージ:** 和風、職人気質、渋くて温かみがある、写真映えする
* **コアバリュー:** 「一杯へのこだわり」を、言葉よりも写真で伝えること。派手さより本物感を優先する

## 2. カラーパレット (Colors)
黒背景をベースに、暖簾（のれん）や器を連想させる朱色と金を差し色として使う。指定された役割（トークン）に応じて、以下のカラーコードを厳密に使用してください。
* **Primary (ブランドカラー・朱色):** `#B7282E` (例: 暖簾の朱)
* **Secondary (アクセント・金):** `#C9A227` (例: 金箔・のれんの縁取り)
* **Background (背景・黒):** `#111010` (例: 墨色に近い黒)
* **Background Sub (セクション背景):** `#1A1918` (例: カード・帯の背景)
* **Text Main (本文テキスト):** `#F5F1EA` (例: 生成り色に近いオフホワイト)
* **Text Muted (薄いテキスト):** `#9C948A` (例: 温かみのあるグレー)
* **Border (境界線):** `#3A3632` (例: 焦げ茶がかったダークグレー)
* **Background Alt (下層ページ用の中間トーン):** `#2E2B28` (例: 木炭のような濃いグレー。こだわり・沿革ページなど、黒だと重すぎるセクションに使用)

## 3. タイポグラフィ (Typography)
見出しには和の風格を出すため明朝体を、本文には可読性を優先しゴシック体を使用する。
* **フォントファミリー（見出し・和文明朝）:** `"Shippori Mincho", "游明朝", "Noto Serif JP", serif`
* **フォントファミリー（本文・和文ゴシック）:** `"Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif`
* **フォントファミリー（英数字）:** `"EB Garamond", "Noto Serif JP", serif`（店名ロゴ等の欧文にのみ使用）
* **ベースフォントサイズ:** `16px` (1rem)
* **見出し 1 (h1):** `3rem` (48px), `line-height: 1.3`, 太字、`letter-spacing: 0.05em`
* **見出し 2 (h2):** `1.75rem` (28px), `line-height: 1.4`, 太字、`letter-spacing: 0.03em`
* **本文 (p):** `1rem` (16px), `line-height: 1.8`（縦書き風の余白感を出すため行間はやや広め）
* **ラベル見出し（英字ラベル＋和文サブタイトル）:** こだわり・歴史などのストーリーセクションで使用。
  * 英字ラベル: `0.875rem` (14px), 大文字, `letter-spacing: 0.15em`, 太字, Secondary（金）または Text Muted
  * 和文サブタイトル: `1.125rem` (18px), 明朝体, 直下に近接配置（余白 `spacing-1`）
  * 例: `KODAWARI / 一杯に込めた想い`、`HISTORY / 店主の歩み`

## 4. スペースとレイアウト (Spacing)
余白や配置は、以下の8pxグリッドシステム（ベースは4px）を基準に設定してください。写真を主役にするため、セクション間の余白は広めに取る。
* `px` (Padding/Margin): `4px`
* `spacing-1` (最小の余白): `8px`
* `spacing-2` (標準的な余白): `16px`
* `spacing-3` (セクション間の余白): `48px`
* `spacing-4` (大きな余白・写真前後): `96px`
* **レイアウト方式:** CSS Grid を優先し、写真をフルブリード（画面幅いっぱい）で配置するセクションを積極的に用いること。

## 5. コンポーネントスタイル (Components)
* **ボタン (Button):**
  * 角丸 (border-radius): `2px`（和風のため角は立たせる。丸め過ぎない）
  * パディング: 縦 `14px`, 横 `32px`
  * 背景: Primary（朱色）、文字色: Text Main
  * シャドウ: `none`
  * ホバー時のエフェクト: 背景色を金（Secondary）に変化させ、`transition: 0.3s`
* **カード (Card / メニューカード):**
  * 背景: `#1A1918` (Background Sub)
  * 角丸 (border-radius): `4px`
  * 境界線: `1px solid [Border Color]`
  * 影 (box-shadow): `none`（暗い背景では影が目立たないため、境界線で区切りを表現する）
  * 写真: カード幅いっぱいに配置し、上部に大きく見せる（比率 4:3 以上）
* **写真セクション (Photo Hero):**
  * 画面幅いっぱいのフルブリード表示
  * 上に薄い黒のグラデーションオーバーレイ（`rgba(0,0,0,0.4)`）を重ね、白文字の可読性を確保
  * 見出しテキストは写真中央〜下部に明朝体で大きく配置
  * バリエーション：見出し英字（例: "The Taste of ○○"）を写真下端に大きくオーバーラップさせ、写真から背景色（Background Alt）へまたがるように配置すると、下層ページの導入として効果的
* **職人紹介ブロック (Founder / Craftsman Block):**
  * モノクロ写真＋右側にテキストの2カラム構成（モバイルでは縦積み）
  * ラベル見出し（英字ラベル＋和文サブタイトル）パターンをタイトルに使用
  * 写真は色を抜く（`filter: grayscale(100%)`）ことで、職人気質・伝統感を強調する
* **カテゴリーフィルター (Category Filter):**
  * ピル型ボタン（`border-radius: 999px`）、境界線のみでシャドウなし
  * 未選択時: 背景透明・Border色の線、選択時: 背景Primary（朱色）・文字White
  * メニュー一覧ページの最上部、見出し直下に配置
* **区切り線 (Divider):**
  * Background Alt のセクション内では、実線ではなく `border-top: 1px dashed [Border Color]` の点線区切りを使用し、和紙や帳面のような質感を演出する
* **メニュー価格表示 (Menu Item):**
  * カード内の写真の下に、商品名（本文サイズ・太字・Text Main）→ 価格（本文サイズ・Text Muted）の順で縦に並べる
  * 価格は「¥900（税込）」のように税込表記を基本とする
* **オーバーラップ情報カード (Overlap Info Card):**
  * 店舗情報・アクセスページ向け。上部に写真を配置し、その下端にカード（Background Sub、角丸 `8px`）を写真へ少し食い込ませるように重ねて配置する（`margin-top: -spacing-4` 相当）
  * カード内の項目（住所・営業時間・定休日など）は、項目名と内容の間に点線区切り線を挟んで縦に並べる
* **写真上の丸型CTAボタン (Floating CTA on Photo):**
  * 「メニューを見る」「予約する」等、写真に重ねて配置する円形または横長ピル型のボタン
  * 背景は白またはText Main、文字はBackground色（写真に負けない高コントラストを優先し、この場合のみPrimary/Secondaryのルールより視認性を優先してよい）
  * 写真の右下または中央下に配置する

## 6. レスポンシブ振る舞い (Responsive)
* **モバイルファースト**で設計すること。来店客の多くはスマホで営業時間・場所を確認するため、店舗情報とアクセスは最優先で見えるようにする。
* **ブレイクポイント:**
  * スマホ (Mobile): `~ 639px`
  * タブレット (Tablet): `640px ~ 1023px`
  * デスクトップ (Desktop): `1024px ~`
* **ナビゲーション（モバイル）:** ヘッダーはロゴを中央または左に固定し、右上に3本線のハンバーガーメニューアイコンを配置。タップでフルスクリーンまたはドロワーのメニューを開く（デスクトップのような横並びナビは640px未満で使用しない）。
* **フッター（モバイル）:** リンクは横並びを廃止し、縦に1列で積み上げる。SNSアイコンは横並びのまま中央寄せ、下部にロゴとコピーライトを配置。
* **見出しの文字サイズ:** モバイルでは見出し1 (h1) を `2rem` (32px) 程度まで縮小し、大きな写真の中でも文字が窮屈にならないようにする。

## 7. アクセシビリティ (Accessibility)
* 黒背景に文字を乗せるデザインのため、すべてのテキスト（特に警告やエラーメッセージ）は、背景色とのコントラスト比を `4.5:1` 以上にすること。
* 写真上に白文字を重ねる場合は、必ず半透明の黒オーバーレイを敷いてコントラストを確保すること。
* 画像には必ず `alt` 属性を記述すること（メニュー写真は料理名を明記）。

## 8. エージェントプロンプトガイド (Agent Prompt)
AIエージェントへの指示です。コードを生成する際は、以下のルールを遵守してください。
1. 上記のトークン（変数名またはカラーコード）をハードコードせず、可能な限りCSS変数またはTailwindのクラスを用いて表現してください。
2. 日本語が不自然にならないよう、文字間隔（`letter-spacing`）や行間を適切に調整してください。特に明朝体見出しは字間を少し広げること。
3. 写真がコンテンツの主役であるため、テキストで説明を埋めすぎず、大きな写真＋短い一言のキャッチコピーという構成を優先してください。
4. 未知のデザイン要素を生成する場合は、事前にユーザーに質問するか、このデザインシステムに準拠した形で提案してください。
5. 下層ページ（こだわり・店主紹介・沿革など）では、黒背景（Background）だけでなく Background Alt（中間トーンのグレー）を使い分け、ページ全体が重くなりすぎないようにしてください。ストーリー系のセクション見出しには「ラベル見出しパターン」を優先的に使用してください。
